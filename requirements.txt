# the CREASE Batting Lab — Python dependencies
# For cloud deployment (Render / Railway), use this file.
# For local dev, either variant works.

# --- Core Analysis Engine ---
opencv-python-headless>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0

# --- Web Framework ---
flask>=2.0.0
gunicorn>=20.0.0
